# Product 5.6.15 Rank Recovery Hotfix

- 記事一覧メニューへ「記事ランクを復旧・再計算」を追加。
- 復旧時は最低表示回数を既定値50として再計算し、異常な設定値の影響を遮断。
- 再計算結果の90%以上が育成になる場合は保存を中止。
- クリック数・表示回数・CTR・掲載順位・作業状態は変更しない。
- 通常の日次処理ではMinImpressionsの異常値を既定値へフォールバック。
