# 製品本体

- `apps-script/Code.gs`：利用者向けApps Script
- `apps-script/appsscript.json`：今回は更新不要

STEP4はページ単位の一括取得方式です。

## 今回の利用手順

1. `apps-script/Code.gs` を上書きします。
2. スプレッドシートを再読み込みします。
3. 管理メニューの「シートを作成・修復」を1回実行します。
4. 「設定」シートの「記事情報補完件数」を30/50/70件から選択します。
5. STEP5を実行し、残件があればダイアログの「続けてN件処理」を使います。
