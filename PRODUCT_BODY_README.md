# Product 5.0 ArticleDB Toolbar

記事DBを中心に操作するための共通ツールバーを追加しました。

## 操作方法

1. 記事DBで対象記事の行を選択します。
2. `SIMS-Blog-Manager → 記事DB → 記事DBツールバーを開く` を実行します。
3. 右側ツールバーから `記事詳細` または `記事を開く` を選びます。

上部メニューの `選択記事の詳細を開く` と `選択記事をブラウザで開く` も引き続き利用できます。

将来の改善ブリーフ、効果測定、改善完了も同じツールバーへ統合する設計です。
