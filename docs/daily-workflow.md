# 日次更新

初回セットアップ完了後は、メニューから **データ更新 → 記事DBを更新（日次）** を実行します。

日次更新ではSearch Consoleの最新ページ指標を取得し、正規化URLをキーに記事DBへ差分反映します。

更新するのはクリック数、表示回数、CTR、掲載順位、記事ステータス、最終取得日時です。記事タイトル、SEOタイトル、メタディスクリプション、メインクエリなどの固定情報は消去・上書きしません。

記事DBに存在しないURLは新規記事として追加され、記事情報補完済みは「×」になります。
